unit AdjustBorders;

interface
uses Vcl.ExtCtrls;

procedure DefineBorders(const AXLast, AYLast: Integer; const AImage: TImage);

implementation

  procedure DefineBorders(const AXLast, AYLast: Integer; const AImage: TImage);
  begin
    AImage.Picture.Bitmap.Width := AXLast + 420;
    AImage.Picture.Bitmap.Height := AYLast + 420;
  end;

end.
